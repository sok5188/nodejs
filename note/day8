express 기초

npm 으로 install 하는 framework이다
framework는 큰 코드를 좀 더 편리하고 안전하게 작성하기 위해 사용한다.

app.get("/", (req, res) => {
  res.send("Hello World!");
});
첫번째인자로 경로, 두번째인자로 경로로 신호를 받게되면 실행할 함수
-- routing(경로지정) 을 하는 것!

동일한 주소로 get방식으로 접근할때와 post방식으로 접근할때의 차이를
두게 할수있음== 다른코드를 실행시킬수있다.
